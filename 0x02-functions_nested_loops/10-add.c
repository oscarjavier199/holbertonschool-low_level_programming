#include "main.h"

/**
 * add - adds two integers.
 * @a: first integer to add.
 * @b: second integer to add.
 * Return: r The result of add.
 */
int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
